import React from 'react';

export default function Icons32Ticket(props) {
    const { src } = props;

    return (
        <div className="icons32-ticket-mobile">
            <img className="union-mobile" src={src} />
        </div>
    );
}